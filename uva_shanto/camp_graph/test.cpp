#include <bits/stdc++.h>
using namespace std;

int main()
{
    int i;

    cin>>i;

    vector<int> v;
    
   // v.push_back(9);

    if(!(!v.size()))
    {
        cout<<"NO/tables have turned bitch\n";
    }
    return 0;
}