#longest substring without repeating characters

class Solution:
    def cleanString(arr):
        length = len(arr)
        while(true):
            for j in range()
                for i in range(1,length-1):
                    ifZZZZ()
